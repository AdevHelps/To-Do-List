package com.example.todolist.application.objects

import android.app.NotificationManager

object NotificationChannelObject {
    const val channelID = "TasksChannelID"
    const val channelName = "Tasks"
    const val importance = NotificationManager.IMPORTANCE_DEFAULT
}